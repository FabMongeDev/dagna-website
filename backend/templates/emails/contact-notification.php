<?php

/**
 * Contact notification email template.
 *
 * Expected variables:
 * - $name
 * - $email
 * - $message
 */

$name = $name ?? '';
$email = $email ?? '';
$message = $message ?? '';

?>

<h2>Nuevo mensaje desde Dagna Website</h2>